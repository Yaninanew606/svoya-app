## Personal Server Context

If a task involves the personal Timeweb server at `72.56.80.106`, read `PERSONAL_SERVER_CONTEXT.md` first.

Use that file as the source of truth for:
- current SSH access details
- which services are expected to be running
- which paths must not be touched
- which old projects were already removed

Do not assume old server state from prior chats if it conflicts with `PERSONAL_SERVER_CONTEXT.md`.
